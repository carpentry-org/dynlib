(load "../../dynlib.carp")

(defn main []
  (println*
    &(=> (DynLib.open "libt.so")
         (Result.and-then
           &(fn [lib] (DynLib.get-from-module lib "Shared" "add")))
         (Result.map &(fn [f] (Int.str (f 1 2)))))))
